@model IEnumerable<IdentityRole>
@{
    ViewBag.Title = "All Roles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@*@if (Model.Any())
{*@
    @*foreach (var role in Model)
        {
            <a class="btn btn-primary" style="width:auto" asp-controller="Administration" asp-action="CreateRole">
                Add new Role
            </a>
            <div class="card mb-3">
                <div class="card-header">
                    Role Id: @role.Id
                </div>
                <div class="card-body">
                    <h5 class="card-title">@role.Name</h5>
                </div>
                <div class="card-footer">
                    <a asp-controller="Administration" asp-action="EditRole" asp-route-id="@role.Id" class="btn btn-primary">Edit</a>
                    <a href="#" class="btn btn-danger">Delete</a>
                </div>
            </div>
        }*@
    <div class="col-xl-4 col-md-4 col-sm-6">
        <div class="card table-card">
            <div class="card-header">
                <h5 class="mb-0">List Role</h5>
                <div class="card-header-right">

                        <a style="float:right" class="btn btn-success mb-2"  asp-action="CreateRole" asp-controller="Administration" > Add new Role</a>
                </div>
            </div>
            <div class="card-body px-0 py-0">
                <div class="table-responsive">
                    <div class="session-scroll" style="position:relative;">
                        <table class="table table-striped">
                            <thead>
                                <tr>

                                    <th>
                                        @Html.DisplayNameFor(model => model.Name)
                                    </th>


                                    <th>
                                        Action
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {

                                    <tr id="tr_@item.Id">

                                        <td>
                                            <a class="pointer-events" onclick="location.href = '@(Url.Action("EditRole", "Administration", new { id = item.Id }))'">@Html.DisplayFor(modelItem => item.Name)</a>                                        
                                        </td>
                                        <td>
                                            <a asp-controller="Administration" asp-action="EditRole" asp-route-id="@item.Id" class="">Edit</a>
                                            @*<a href="#" >Delete</a>*@
                                        </td>                                     
                                    </tr>
                                }
                            </tbody>
                        </table>

                    </div>
                </div>
            </div>
        </div>
    </div>
@*}
else
{
    <div class="card">
        <div class="card-header">
            No roles create yet
        </div>
        <div class="card-body">
            <h5 class="card-title">
                Use the button below to create a role
            </h5>
            <a class="btn btn-primary" style="width:auto" asp-controller="Administration" asp-action="CreateRole">
                Add new Role
            </a>
        </div>

    </div>
}*@
